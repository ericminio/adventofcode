const { input } = require('../support');

const solve1 = (file) => {
    const data = input(file);
    for (var i = 4; i < data.length; i++) {
        let marker = {}
    }
    return 7;
}

module.exports = { solve1 };