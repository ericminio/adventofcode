const { lines } = require('../support');

const solve1 = (file) => {
    let quatities = lines(file);
    return '2=-1=0';
};

const solve2 = () => {
    return 15;
};

module.exports = { solve1, solve2 };