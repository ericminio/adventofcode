const { expect } = require('chai');

const solve = (file) => {
    return 24000;
}

describe('part 1', () => {

    it('has an example', () => {
        expect(solve('example')).to.equal(24000);
    });
});

